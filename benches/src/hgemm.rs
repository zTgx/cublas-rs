// HGEMM benchmarks

/// Run HGEMM benchmarks comparing scalar f16 vs Tensor Core implementations.
pub fn bench_hgemm(m: usize, n: usize, k: usize) {
    let _ = (m, n, k);
    todo!("implement HGEMM benchmarks")
}
