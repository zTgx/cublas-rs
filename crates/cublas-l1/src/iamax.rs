// IAMAX: argmax of |x[i]|, returns the index (0-based) of largest |x[i]|

/// ISAMAX — index of element with maximum absolute value.
pub fn isamax(n: usize, x: &[f32]) -> usize {
    let _ = (n, x);
    todo!("launch ISAMAX kernel")
}
