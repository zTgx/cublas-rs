// DOT: dot product of two vectors

/// DOT kernel launch.
///
/// Returns the dot product sum(x[i] * y[i]) for i in 0..n.
pub fn dot(n: usize, x: &[f32], y: &[f32]) -> f32 {
    let _ = (n, x, y);
    todo!("launch DOT kernel")
}
